from tools import *

class CodingAgent:
    """
    A coding agent that can solve programming tasks.
    """
    
    def __init__(self, model: str):
        self.model: str = model
        
    def run(self, question: str) -> str:
        """
        Run the agent on a given question.

        Args:
            question (str): The question to run the agent on.

        Returns:
            str: The output of the agent on the task.
        """
        pass
    